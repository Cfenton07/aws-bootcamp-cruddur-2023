#! /usr/bin/bash

ABS_PATH=$(readlink -f "$0")
CDK_PATH=$(dirname $ABS_PATH)/../../thumbnail-serverless-cdk

echo ">>> CDK Destroy: Tearing down stack..."
cd $CDK_PATH
cdk destroy --force
echo ">>> CDK Destroy complete."
