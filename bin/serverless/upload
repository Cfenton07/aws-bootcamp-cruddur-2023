#! /usr/bin/bash

# Upload a test image to the uploads bucket to trigger the Lambda pipeline

ABS_PATH=$(readlink -f "$0")
SERVERLESS_PATH=$(dirname "$ABS_PATH")
BIN_PATH=$(dirname "$SERVERLESS_PATH")
PROJECT_PATH=$(dirname "$BIN_PATH")
DATA_FILE_PATH="$PROJECT_PATH/journal/assets/vh40t.jpg"

aws s3 cp "$DATA_FILE_PATH" "s3://fentoncruddur-uploaded-avatars/avatars/original/data.jpg"